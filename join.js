//método1: join = Inverte a ordem dos elementos de um array.

//exemplo 1
let músicas = ["Sing of the Times", "Lights Up", "Satellite", "Fine Line"]; //declara a variavél
let resultado = músicas.join (", "); //inverte a ordem
console.log(resultado); //exibe a mensagem (resultado)

//exemplo 2
let comidas = ["Hambúrguer", "Tacos", "Maionese", "Bife"]; //declara a variavél
let final = comidas.join (", "); //inverte a ordem
console.log(final); //exibe a mensagem (resultado)
